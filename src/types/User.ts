export interface User {
  id: string
  email: string
  name: string
}

export interface UserItem {
  id: string
  email: string
  name: string,
  cellphone_number: string,
  cellphone_prefix: string,
  cellphone: string
}